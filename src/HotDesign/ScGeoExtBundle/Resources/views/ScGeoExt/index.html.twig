<h1>ScGeoExt list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Lat</th>
            <th>Lng</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('scgeoext_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.lat }}</td>
            <td>{{ entity.lng }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('scgeoext_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('scgeoext_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('scgeoext_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
