{% extends 'SimpleCatalogBundle:Layouts:base.html.twig' %}
{% form_theme edit_form 'SimpleCatalogBundle:Form:fields.html.twig' %}

{% block title %} Editando {{ entity }} {% endblock %}

{% block content %}
<h2>{{ block('title') }}</h2>

<form action="{{ path('category_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}


        <div class="actions">
            <input type="submit" class="btn primary" value="Guardar">
            <a href="{{ path('category') }}" class="btn small">Ir al Listado</a>
            <form action="{{ path('category_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
                    <button type="submit" class="btn danger">Borrar</button>
                </form>
            </div>

        </form>


{% endblock %}